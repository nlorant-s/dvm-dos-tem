# Copy to .env and set paths for your machine (do not commit .env):
#   cp .env.example .env
#
# Used by docker-compose.yml volume binds.

DDT_INPUT_CATALOG=/path/to/your/input-catalog
DDT_WORKFLOWS=/path/to/your/workflows
V_TAG=local
